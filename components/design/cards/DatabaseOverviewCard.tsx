import React from 'react';
import { DesignCard, DesignCardProps } from './DesignCard';

/**
 * データベース構成のカードコンポーネント
 */
export function DatabaseOverviewCard(props: DesignCardProps) {
  return (
    <DesignCard {...props} />
  );
}
