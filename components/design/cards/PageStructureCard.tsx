import React from 'react';
import { DesignCard, DesignCardProps } from './DesignCard';

/**
 * ページ構造のカードコンポーネント
 */
export function PageStructureCard(props: DesignCardProps) {
  return (
    <DesignCard {...props} />
  );
}
