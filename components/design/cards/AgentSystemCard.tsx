import React from 'react';
import { DesignCard, DesignCardProps } from './DesignCard';

/**
 * Agentシステムのカードコンポーネント
 */
export function AgentSystemCard(props: DesignCardProps) {
  return (
    <DesignCard {...props} />
  );
}

