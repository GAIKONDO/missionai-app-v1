# MissionAI 配布パッケージ

## インストール手順

### 1. アプリケーションのインストール

1. `MissionAI_2.1.2_aarch64.dmg`をダブルクリックしてマウント
2. MissionAI.appをアプリケーションフォルダにドラッグ&ドロップ
3. アプリケーションを起動

### 2. 依存関係のインストール（初回のみ）

MissionAIアプリケーションを使用するには、以下の依存関係が必要です：

- **Java 8以上**: PlantUML図のレンダリングに必要
  - **注意**: PlantUML JARファイルは既にアプリにバンドルされているため、追加でPlantUMLをインストールする必要はありません。Javaさえインストールされていれば、PlantUML図は正常にレンダリングされます。
- **Graphviz**: PlantUML図の一部の図タイプ（クラス図、シーケンス図など）をレンダリングするために必要
- **Python 3.8-3.12**: ChromaDB（ベクトル検索）に必要
- **ChromaDB**: ベクトル検索機能に必要（Pythonパッケージ）

#### 簡単な方法：自動インストールスクリプトを使用

**推奨**: `install-dependencies-auto.command`をダブルクリックするだけです。

**実行方法（2つの方法）:**

**方法1: ダブルクリックで実行**
1. `install-dependencies-auto.command`ファイルをダブルクリック
2. ターミナルが開き、必要な依存関係が自動的にインストールされます
3. インストール完了後、Enterキーを押して終了してください

**方法2: ターミナルから実行（権限エラーが出る場合）**
もし「適切なアクセス権限がないため実行できません」というエラーが出る場合：

1. ターミナルを開く（アプリケーション > ユーティリティ > ターミナル）
2. 以下のコマンドを実行（ファイルをターミナルにドラッグ&ドロップするとパスが自動入力されます）:
   ```bash
   chmod +x install-dependencies-auto.command
   ./install-dependencies-auto.command
   ```

**注意**: 
- 初回実行時、Homebrewのインストールが必要な場合があります（数分かかります）
- 管理者パスワードの入力が求められる場合があります
- 外部からダウンロードしたファイルの場合、macOSのセキュリティ機能により実行権限が制限されることがあります

#### 対話型インストール（確認しながらインストール）

各依存関係について確認しながらインストールしたい場合：

1. `install-dependencies.command`ファイルをダブルクリック
2. 各依存関係について、インストールの確認を求められます
3. 「y」を入力してEnterキーを押すと、インストールが開始されます

## 機能の確認

### PlantUML図のレンダリング

Javaがインストールされている場合、PlantUML図が正常にレンダリングされます。

### ベクトル検索機能

ChromaDBがインストールされている場合、ベクトル検索機能（RAG検索）が使用できます。

## トラブルシューティング

### Javaが見つからない

- ターミナルを再起動してください
- `java -version`コマンドでJavaがインストールされているか確認してください
- インストールスクリプトを再実行してください

### ChromaDBが動作しない

- Python 3.8-3.12がインストールされているか確認してください
- `python3 -c "import chromadb"`コマンドでChromaDBがインストールされているか確認してください
- インストールスクリプトを再実行してください

### その他の問題

詳細なトラブルシューティング情報は、`README_INSTALL.md`を参照してください。

## サポート

問題が発生した場合は、以下のドキュメントを参照してください：

- [インストールガイド](README_INSTALL.md)
- [ユーザーセットアップガイド](docs/setup/USER_SETUP_GUIDE.md)
- [トラブルシューティングガイド](docs/troubleshooting/TROUBLESHOOTING.md)

## ファイル構成

配布パッケージには以下のファイルが含まれています：

- `MissionAI_2.1.2_aarch64.dmg`: アプリケーション本体
- `install-dependencies-auto.command`: 自動インストールスクリプト（推奨）
- `install-dependencies.command`: 対話型インストールスクリプト
- `README_INSTALL.md`: 詳細なインストールガイド
- `DISTRIBUTION_README.md`: このファイル

