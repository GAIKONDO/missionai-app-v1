# Agentシステム実装計画書

> **📋 ステータス**: 計画中  
> **📅 作成日**: 2025-01-27  
> **👤 用途**: マルチエージェントシステムの実装計画と進捗管理

## 📋 概要

### 目的
MissionAIアプリケーションに、複数のAI Agentが協調してタスクを実行できるマルチエージェントシステムを実装する。AgentはMCP（Model Context Protocol）のToolとして実装し、Agent間の通信にはA2A（Agent-to-Agent）プロトコルを使用するハイブリッド方式を採用する。

### 主要機能
- **タスク管理**: 様々なタスクを登録し、Agentが自動実行
- **マルチエージェント協調**: 複数のAgentが協調して複雑なタスクを実行
- **Agent間通信**: A2Aプロトコルによる確認、指示、通知
- **タスクオーケストレーション**: タスクの依存関係を解決し、最適な実行順序を決定
- **実行履歴と監視**: タスク実行履歴の記録と監視機能

---

## 🏗️ アーキテクチャ設計

### 全体構成

```
┌─────────────────────────────────────────────────────────────┐
│                    プレゼンテーション層                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ タスク管理UI │  │ Agent管理UI  │  │ 実行監視UI   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  オーケストレーション層                        │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         AgentOrchestrator（タスク配分・管理）          │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         TaskPlanner（実行計画作成・依存関係解決）       │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      Agent層                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Agent A      │  │ Agent B      │  │ Agent C      │     │
│  │ (Tool実装)   │  │ (Tool実装)   │  │ (Tool実装)   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│         ↓                ↓                ↓                │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         A2AManager（Agent間通信管理）                 │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      MCP層                                    │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         MCPToolRegistry（AgentをToolとして登録）       │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      データ層                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ SQLite       │  │ ChromaDB     │  │ 実行履歴DB   │     │
│  │ (タスク定義) │  │ (RAG検索)    │  │ (ログ)       │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### コンポーネント構成

#### 1. AgentOrchestrator
- **役割**: タスクの受付、Agentへの配分、実行管理
- **機能**:
  - タスクの受付と検証
  - 実行計画の作成（TaskPlannerに委譲）
  - Agentへのタスク配分
  - 実行状態の監視
  - 結果の統合

#### 2. TaskPlanner
- **役割**: タスクの依存関係を分析し、実行計画を作成
- **機能**:
  - タスクの依存関係解析
  - 実行順序の決定（トポロジカルソート）
  - 並列実行可能なタスクの特定
  - リソース制約の考慮

#### 3. Agent
- **役割**: 個別のタスクを実行するAIエージェント
- **機能**:
  - タスクの受領と実行
  - Toolの呼び出し
  - 他のAgentとの通信（A2A経由）
  - 実行結果の報告

#### 4. A2AManager
- **役割**: Agent間の通信を管理
- **機能**:
  - メッセージの送受信
  - 確認要求の処理
  - 通知の配信
  - 通信履歴の記録

---

## 📊 データ構造

### タスク定義（Task）

```typescript
interface Task {
  id: string;                    // タスクID
  name: string;                  // タスク名
  description: string;           // タスクの説明
  type: TaskType;                // タスクタイプ
  agentId?: string;              // 実行Agent（指定時）
  requiredAgents?: string[];     // 必要なAgentリスト
  dependencies?: string[];       // 依存タスクIDリスト
  parameters: Record<string, any>; // タスクパラメータ
  priority: number;              // 優先度（1-10）
  timeout?: number;              // タイムアウト（ミリ秒）
  retryCount?: number;           // リトライ回数
  createdAt: number;             // 作成日時
  updatedAt: number;             // 更新日時
}

enum TaskType {
  SEARCH = 'search',              // 検索タスク
  ANALYSIS = 'analysis',          // 分析タスク
  GENERATION = 'generation',      // 生成タスク
  VALIDATION = 'validation',      // 検証タスク
  COORDINATION = 'coordination',   // 協調タスク
}
```

### タスク実行（TaskExecution）

```typescript
interface TaskExecution {
  id: string;                    // 実行ID
  taskId: string;                 // タスクID
  agentId: string;               // 実行Agent ID
  status: ExecutionStatus;       // 実行状態
  startedAt: number;             // 開始日時
  completedAt?: number;          // 完了日時
  result?: any;                  // 実行結果
  error?: string;                // エラーメッセージ
  logs: ExecutionLog[];          // 実行ログ
}

enum ExecutionStatus {
  PENDING = 'pending',           // 待機中
  RUNNING = 'running',           // 実行中
  COMPLETED = 'completed',       // 完了
  FAILED = 'failed',             // 失敗
  CANCELLED = 'cancelled',       // キャンセル
}

interface ExecutionLog {
  timestamp: number;             // ログタイムスタンプ
  level: 'info' | 'warn' | 'error'; // ログレベル
  message: string;               // ログメッセージ
  data?: any;                    // 追加データ
}
```

### Agent定義（Agent）

```typescript
interface Agent {
  id: string;                    // Agent ID
  name: string;                  // Agent名
  description: string;           // Agentの説明
  role: AgentRole;               // Agentの役割
  capabilities: string[];        // 実行可能なタスクタイプ
  tools: string[];               // 利用可能なToolリスト
  modelType: ModelType;          // 使用するLLM
  systemPrompt: string;           // システムプロンプト
  config: AgentConfig;           // Agent設定
  createdAt: number;             // 作成日時
  updatedAt: number;             // 更新日時
}

enum AgentRole {
  SEARCHER = 'searcher',          // 検索専門
  ANALYZER = 'analyzer',          // 分析専門
  GENERATOR = 'generator',        // 生成専門
  VALIDATOR = 'validator',        // 検証専門
  COORDINATOR = 'coordinator',    // 協調専門
  GENERAL = 'general',            // 汎用
}

interface AgentConfig {
  maxConcurrentTasks: number;     // 同時実行可能タスク数
  defaultTimeout: number;         // デフォルトタイムアウト
  retryPolicy: RetryPolicy;       // リトライポリシー
}

interface RetryPolicy {
  maxRetries: number;             // 最大リトライ回数
  retryDelay: number;             // リトライ遅延（ミリ秒）
  backoffMultiplier: number;      // バックオフ倍率
}
```

### A2Aメッセージ（A2AMessage）

```typescript
interface A2AMessage {
  id: string;                    // メッセージID
  from: string;                  // 送信元Agent ID
  to: string;                    // 送信先Agent ID
  type: A2AMessageType;          // メッセージタイプ
  taskId?: string;               // 関連タスクID
  payload: any;                 // メッセージペイロード
  timestamp: number;             // 送信日時
  responseTo?: string;           // 応答先メッセージID
  requiresResponse: boolean;      // 応答が必要か
}

enum A2AMessageType {
  REQUEST = 'request',            // リクエスト
  RESPONSE = 'response',          // レスポンス
  NOTIFICATION = 'notification',   // 通知
  CONFIRMATION = 'confirmation',  // 確認要求
  STATUS_UPDATE = 'status_update', // 状態更新
}
```

---

## 🚀 実装フェーズ

### フェーズ1: 基盤構築（1-2週間）

#### 1.1 データ構造の定義
- [ ] TypeScript型定義の作成
- [ ] SQLiteスキーマの設計と実装
- [ ] データアクセス層の実装

#### 1.2 A2Aプロトコルの実装
- [ ] `A2AManager`クラスの実装
- [ ] メッセージ送受信機能
- [ ] 確認要求機能
- [ ] 通知機能
- [ ] 通信履歴の記録

#### 1.3 Agent基盤クラスの実装
- [ ] `Agent`基底クラスの実装
- [ ] Agent登録・管理機能
- [ ] Agent設定の管理

**成果物**:
- `lib/agent-system/types.ts`
- `lib/agent-system/a2aManager.ts`
- `lib/agent-system/agent.ts`
- `lib/agent-system/agentRegistry.ts`
- `src-tauri/src/database/agent_system.rs`（SQLiteスキーマ）

---

### フェーズ2: オーケストレーション層の実装（1-2週間）

#### 2.1 TaskPlannerの実装
- [ ] 依存関係解析機能
- [ ] トポロジカルソート実装
- [ ] 並列実行可能タスクの特定
- [ ] 実行計画の生成

#### 2.2 AgentOrchestratorの実装
- [ ] タスクの受付と検証
- [ ] 実行計画の作成（TaskPlanner連携）
- [ ] Agentへのタスク配分
- [ ] 実行状態の監視
- [ ] 結果の統合

#### 2.3 タスク管理機能
- [ ] タスクのCRUD操作
- [ ] タスク実行の開始・停止・キャンセル
- [ ] 実行履歴の記録と取得

**成果物**:
- `lib/agent-system/taskPlanner.ts`
- `lib/agent-system/agentOrchestrator.ts`
- `lib/agent-system/taskManager.ts`

---

### フェーズ3: Agent実装（2-3週間）

#### 3.1 基本Agentの実装
- [ ] `SearchAgent`: 検索タスク専用Agent
- [ ] `AnalysisAgent`: 分析タスク専用Agent
- [ ] `GenerationAgent`: 生成タスク専用Agent
- [ ] `ValidationAgent`: 検証タスク専用Agent
- [ ] `GeneralAgent`: 汎用Agent

#### 3.2 AgentをMCP Toolとして登録
- [ ] Agentを`MCPToolImplementation`として実装
- [ ] Toolレジストリへの登録
- [ ] LLMからのAgent呼び出し対応

#### 3.3 Agent間通信の実装
- [ ] Agent間の確認要求機能
- [ ] Agent間の指示出し機能
- [ ] Agent間の通知機能

**成果物**:
- `lib/agent-system/agents/SearchAgent.ts`
- `lib/agent-system/agents/AnalysisAgent.ts`
- `lib/agent-system/agents/GenerationAgent.ts`
- `lib/agent-system/agents/ValidationAgent.ts`
- `lib/agent-system/agents/GeneralAgent.ts`
- `lib/mcp/tools/agentTools.ts`

---

### フェーズ4: UI実装（1-2週間）

#### 4.1 タスク管理UI
- [ ] タスク一覧ページ
- [ ] タスク作成・編集フォーム
- [ ] タスク詳細表示
- [ ] タスク実行履歴表示

#### 4.2 Agent管理UI
- [ ] Agent一覧ページ
- [ ] Agent作成・編集フォーム
- [ ] Agent設定画面
- [ ] Agent状態監視

#### 4.3 実行監視UI
- [ ] 実行中タスクの監視
- [ ] 実行ログの表示
- [ ] Agent間通信の可視化
- [ ] パフォーマンスメトリクス表示

**成果物**:
- `app/tasks/page.tsx`
- `app/tasks/[id]/page.tsx`
- `app/agents/page.tsx`
- `app/agents/[id]/page.tsx`
- `app/executions/page.tsx`
- `components/tasks/`
- `components/agents/`
- `components/executions/`

---

### フェーズ5: 高度な機能（2-3週間）

#### 5.1 タスクテンプレート機能
- [ ] タスクテンプレートの定義
- [ ] テンプレートからのタスク生成
- [ ] テンプレートライブラリ

#### 5.2 タスクチェーン機能
- [ ] 複数タスクの連鎖実行
- [ ] 条件分岐対応
- [ ] ループ処理対応

#### 5.3 エラーハンドリングとリトライ
- [ ] 自動リトライ機能
- [ ] エラー通知機能
- [ ] フォールバック処理

#### 5.4 パフォーマンス最適化
- [ ] 並列実行の最適化
- [ ] キャッシング機能
- [ ] リソース管理

**成果物**:
- `lib/agent-system/taskTemplates.ts`
- `lib/agent-system/taskChain.ts`
- `lib/agent-system/errorHandler.ts`
- `lib/agent-system/performanceOptimizer.ts`

---

## 🛠️ 技術スタック

### 既存技術の活用
- **TypeScript**: 型安全性の確保
- **React**: UI実装
- **Next.js**: ページルーティング
- **SQLite**: タスク・Agent定義の永続化
- **MCP**: ToolとしてのAgent実装
- **LLM API**: Agentの思考・実行

### 新規追加技術
- **グラフ理論**: 依存関係解析（トポロジカルソート）
- **イベントエミッター**: Agent間通信の非同期処理
- **状態管理**: 実行状態の管理（ZustandまたはContext API）

---

## 📝 実装詳細

### A2AManager実装例

```typescript
class A2AManager {
  private agents: Map<string, Agent> = new Map();
  private messageQueue: A2AMessage[] = [];
  private messageHistory: Map<string, A2AMessage> = new Map();
  
  /**
   * Agentを登録
   */
  registerAgent(agent: Agent): void {
    this.agents.set(agent.id, agent);
  }
  
  /**
   * メッセージを送信
   */
  async sendMessage(message: A2AMessage): Promise<A2AMessage | null> {
    const targetAgent = this.agents.get(message.to);
    if (!targetAgent) {
      throw new Error(`Agent ${message.to} not found`);
    }
    
    // メッセージを履歴に記録
    this.messageHistory.set(message.id, message);
    
    // Agentにメッセージを配信
    const response = await targetAgent.handleMessage(message);
    
    if (response) {
      this.messageHistory.set(response.id, response);
    }
    
    return response;
  }
  
  /**
   * 確認要求を送信
   */
  async requestConfirmation(
    from: string,
    to: string,
    question: string,
    taskId?: string
  ): Promise<boolean> {
    const message: A2AMessage = {
      id: generateId(),
      from,
      to,
      type: A2AMessageType.CONFIRMATION,
      taskId,
      payload: { question },
      timestamp: Date.now(),
      requiresResponse: true,
    };
    
    const response = await this.sendMessage(message);
    return response?.payload.confirmed === true;
  }
}
```

### AgentOrchestrator実装例

```typescript
class AgentOrchestrator {
  private taskPlanner: TaskPlanner;
  private a2aManager: A2AManager;
  private agentRegistry: AgentRegistry;
  private executions: Map<string, TaskExecution> = new Map();
  
  /**
   * タスクを実行
   */
  async executeTask(task: Task): Promise<TaskExecution> {
    // 1. 実行計画を作成
    const plan = await this.taskPlanner.createPlan(task);
    
    // 2. 実行IDを生成
    const executionId = generateId();
    
    // 3. 実行記録を作成
    const execution: TaskExecution = {
      id: executionId,
      taskId: task.id,
      agentId: plan.assignedAgentId,
      status: ExecutionStatus.PENDING,
      startedAt: Date.now(),
      logs: [],
    };
    
    this.executions.set(executionId, execution);
    
    // 4. Agentにタスクを配分
    const agent = this.agentRegistry.get(plan.assignedAgentId);
    if (!agent) {
      throw new Error(`Agent ${plan.assignedAgentId} not found`);
    }
    
    // 5. タスクを実行
    execution.status = ExecutionStatus.RUNNING;
    try {
      const result = await agent.executeTask(task, {
        executionId,
        a2aManager: this.a2aManager,
      });
      
      execution.status = ExecutionStatus.COMPLETED;
      execution.completedAt = Date.now();
      execution.result = result;
    } catch (error: any) {
      execution.status = ExecutionStatus.FAILED;
      execution.completedAt = Date.now();
      execution.error = error.message;
    }
    
    return execution;
  }
  
  /**
   * 複数タスクを実行（依存関係を考慮）
   */
  async executeTasks(tasks: Task[]): Promise<TaskExecution[]> {
    // 1. 実行計画を作成
    const plan = await this.taskPlanner.createExecutionPlan(tasks);
    
    // 2. 順次実行と並列実行を処理
    const results: TaskExecution[] = [];
    
    for (const stage of plan.stages) {
      // 並列実行可能なタスクを同時に実行
      const stageResults = await Promise.all(
        stage.tasks.map(task => this.executeTask(task))
      );
      results.push(...stageResults);
    }
    
    return results;
  }
}
```

---

## 🧪 テスト計画

### 単体テスト
- [ ] A2AManagerのテスト
- [ ] TaskPlannerのテスト
- [ ] AgentOrchestratorのテスト
- [ ] 各Agentのテスト

### 統合テスト
- [ ] Agent間通信のテスト
- [ ] タスク実行フローのテスト
- [ ] 依存関係解決のテスト

### E2Eテスト
- [ ] UIからのタスク作成・実行
- [ ] Agent間の確認要求フロー
- [ ] エラーハンドリングとリトライ

---

## 📈 成功指標

### 機能面
- ✅ 複数のAgentが協調してタスクを実行できる
- ✅ Agent間の確認・指示が正常に動作する
- ✅ タスクの依存関係が正しく解決される
- ✅ 実行履歴が正しく記録される

### パフォーマンス面
- ✅ 並列実行により実行時間が短縮される
- ✅ メッセージ送受信のレイテンシが低い（<100ms）
- ✅ 大量のタスクを効率的に処理できる

### ユーザビリティ面
- ✅ UIが直感的で使いやすい
- ✅ 実行状態が視覚的に分かりやすい
- ✅ エラーが適切に表示される

---

## 🔄 今後の拡張案

### 短期（3-6ヶ月）
- Agentの学習機能（実行結果から改善）
- タスクの自動最適化
- より高度な依存関係解析

### 中期（6-12ヶ月）
- Agentの動的生成
- タスクの自動発見
- マルチテナント対応

### 長期（12ヶ月以上）
- 分散Agentシステム
- Agent間の競合解決
- 自己改善するAgentシステム

---

## 📚 参考資料

### 関連ドキュメント
- [MCP実装状況](./../design/DESIGN_DOCUMENT_SYSTEM.md)
- [RAG検索の仕組み](./../rag-search/SEARCH_DATABASE_FLOW.md)
- [オーケストレーション・MCP・LLM連携](./../design/DESIGN_DOCUMENT_SYSTEM.md)

### 外部リソース
- Model Context Protocol (MCP) 仕様
- マルチエージェントシステムの設計パターン
- タスクスケジューリングアルゴリズム

---

## 📝 変更履歴

| 日付 | 変更内容 | 担当者 |
|------|---------|--------|
| 2025-01-27 | 初版作成 | - |

